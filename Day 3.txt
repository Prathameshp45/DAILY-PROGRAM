def duplicate(arr):
    n=len(arr)
    for i in range(0,n):
        for j in range(i+1,n):
            if (arr[i] == arr[j]):
                print("Duplicate number: ",arr[i])
arr=list(map(int, input().split()))
duplicate(arr)